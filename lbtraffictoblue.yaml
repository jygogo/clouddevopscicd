apiVersion: v1
kind: Service
metadata:
  name: my-loadbalancer
  labels:
    app: my-loadbalancer
spec:
  selector:
    app: blue
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 80
  clusterIP: 10.100.0.1
  type: LoadBalancer
